pre-commit:
  commands:
    format:
      glob: "*.{nix}"
      run: nixfmt {staged_files}
      stage_fixed: true
pre-push:
  parallel: true
  commands:
    check:
      glob: "*.{nix,json,lock}"
      run: |
        nix flake check
      skip:
        - merge
